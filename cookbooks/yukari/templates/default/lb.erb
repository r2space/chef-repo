upstream node {
    <% @hosts.each do |host| %>
    server <%= host['name'] %>;
    <% end %>
}

server {
    listen       80;
    server_name  localhost;
    client_max_body_size 1024M;
    client_body_buffer_size 100M;
    
    location / {
        proxy_pass http://node;
    }
}
